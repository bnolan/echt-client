machine:
  node:
    version: v6.1.0
dependencies:
  override:
    - yarn:
        pwd:
          server
    - yarn:
        pwd:
          Echt
test:
  override:
    - yarn test:
        pwd:
          server
    - yarn lint:
        pwd:
          server
    - yarn lint:
        pwd:
          Echt
